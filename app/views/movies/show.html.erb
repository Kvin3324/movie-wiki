<style media="screen">
  img {
    max-width: 200px;
    max-height: 200px;
  }
</style>


<p id="notice"><%= notice %></p>

<h1><%= @movie.title %></h1>

<p>
  <strong>Synopsis:</strong>
  <%= image_tag @movie.image_url, align: 'right' %>
  <%= @movie.description %>
</p>

<p>
  <strong>Teaser:</strong>
  <%= @movie.teaser %>
</p>

  <%= @movie.rating %>

<%= if user_signed_in? %>
<%= link_to 'Edit', edit_movie_path(@movie) %> 
<% end %>
<%= link_to 'Back', movies_path %>
